
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:,/dev.db" 
}

model company {
  id Int @id @default(autoincrement())
  name String  
  img String
  position String
  detail String
}